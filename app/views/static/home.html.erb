<section class="py-5">
  <div class="container">
    <div class="row">
      <div class="col-sm">
        <h3>New Publications</h3>
        <ul class="list-unstyled">
          <% @newpubs.each do |p| %>
            <li><%= link_to(p.title, publication_path(p)) %></li>
          <% end %>
        </ul>
      </div>
      <div class="col-sm">2</div>
      <div class="col-sm">3</div>
    </div>
  </div>
</section>
